# 버그 리포트 — 졸업전시회 전 수정 목록

## 우선순위 요약

| 순위 | 버그 | 심각도 | 수정 난이도 |
|------|------|--------|------------|
| P1 | 스트립 점수/등급 불일치 | 🔴 높음 | 쉬움 |
| P1 | "이런 업종은 어때요?" 성장확률 숫자 미표시 | 🔴 높음 | 쉬움 |
| P2 | 종합점수 A등급 불가 | 🟠 중간 | 매우 쉬움 |
| P3 | 빈 결과 4개 조합 | 🟡 낮음 | 어려움 (파이프라인 재실행) |

---

## P1 — 스트립 점수/등급 불일치 🔴

### 증상
- "이런 지역은 어때요?" 스트립에서 **67점 A등급**으로 표시된 돈암2동을
- 클릭해서 들어가면 **51점 C등급**으로 나옴

### 원인
두 가지 문제가 섞여 있음:

1. **점수 불일치**: 스트립의 점수는 `/api/recommend/location/` 의 **입지점수** 기준,  
   실제 클릭 후 점수는 `/api/recommend/score/` 의 **종합점수** 기준으로 계산 방식이 다름

2. **등급 불일치**: 스트립의 A등급은 `ScoreData.등급` (성장확률 기준 전국 상위 25%) 을 그대로 표시,  
   종합점수 패널의 등급은 composite 점수 기준 (75점 이상 = A) 으로 기준이 다름

### 해결 방법 (택 1)

**방법 A (권장) — 스트립 등급 제거**  
스트립 카드에서 A/B/C/D 등급 뱃지를 제거하고 성장확률(%)만 표시  
→ 프론트엔드 `MapPage.jsx` 스트립 카드 렌더링 부분에서 등급 뱃지 숨기기

**방법 B — 레이블 구분**  
스트립의 등급을 "AI등급" 대신 "성장등급"으로 레이블 변경하여 종합점수 등급과 구분

---

## P2 — 종합점수 A등급 불가 🟠

### 증상
- 어떤 행정동×업종 조합을 검색해도 종합점수 A등급이 나오지 않음
- 가장 높은 점수: 한식 종로1.2.3.4가동 74.4점 (A 기준 75점에서 0.6점 부족)

### 원인
종합점수 A등급 기준이 75점으로 너무 높음.  
성장확률이 높은 지역은 매출이 낮고, 매출이 높은 강남·종로는 경쟁강도가 높아서  
두 조건을 동시에 만족해 75점을 넘기기 구조적으로 어려움.

### 해결 방법

`backend/analysis/views.py` 1337~1342줄 기준값 수정:

```python
# 현재
grade = (
    "A" if composite >= 75 else
    "B" if composite >= 55 else
    "C" if composite >= 35 else
    "D"
)

# 수정 후
grade = (
    "A" if composite >= 65 else
    "B" if composite >= 50 else
    "C" if composite >= 35 else
    "D"
)
```

→ 수정 후 한식 종로1.2.3.4가동(74.4), 카페 종로1.2.3.4가동(68.2) 등 A등급 정상 출력

---

## P1 — "이런 업종은 어때요?" 성장확률 숫자 미표시 🔴

### 증상
- score 패널 하단 "이런 업종은 어때요?" 스트립 카드에서
- `성장확률 %` 앞에 숫자가 없이 **"성장확률 %"** 로만 표시됨

### 원인
`/api/recommend/industry/` 응답 데이터에서 성장확률 값이 스트립 카드로 전달되지 않거나,
프론트에서 해당 필드명을 잘못 참조하고 있음

### 해결 방법
`MapPage.jsx` "이런 업종은 어때요?" 스트립 렌더링 부분에서
성장확률 필드명 확인 후 수정
- API 응답 키 확인: `성장확률` or `성장확률_pct` or `growth_rate` 등
- 해당 필드를 카드에 올바르게 바인딩

---

## P3 — 빈 결과 4개 조합 🟡

### 증상
아래 4개 조합에서 AI 추천 행정동 결과가 아무것도 나오지 않음

| 구 | 업종 |
|----|------|
| 종로구 | 기타 B2B서비스 |
| 금천구 | 외국어학원 |
| 금천구 | 컴퓨터및주변장치판매 |
| 강동구 | 가방 |

### 원인
`ScoreData`에 해당 구의 행정동×업종 조합이 없어서 교집합이 0개가 됨

### 해결 방법

**코드 수정 (임시 대응)**: 결과가 0개일 때 구 필터를 제거하고 서울 전체 기준으로 재조회하는 폴백 로직 추가

`backend/analysis/views.py` `recommend_location()` 함수 내:
```python
# 현재: 결과 없으면 빈 배열 반환
# 수정: 결과 0개일 때 gu 필터 없이 재조회 후 반환
if len(result) == 0:
    # gu 필터 제거하고 전국 기준으로 재조회
    result = ... (gu 조건 없이 동일 로직 반복)
    is_fallback = True  # 프론트에 fallback 표시용
```

**근본 해결**: AI 파이프라인 재실행 (시간 소요)
```bash
cd ai
python enrich_dong_dataset.py
python retrain_scores.py
cd ../backend
python manage.py import_scores
```
